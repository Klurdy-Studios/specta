# Skills public API

- createSkillGenerator(manifestRepository?, fileSystem?) generates deterministic
  native Skill packages from Workflow Definitions.

Generated Skills contain workflow metadata and references only. They do not
contain Specta business logic.
